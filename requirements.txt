feedparser==6.0.11
requests==2.32.3
python-dotenv==1.0.1
trafilatura==2.0.0
fastapi==0.115.12
uvicorn[standard]==0.34.0
jinja2==3.1.6
python-multipart==0.0.20
itsdangerous==2.2.0
slowapi==0.1.9
# Finnish lemmatisation for tap-to-translate. Pure-Python ctypes binding; needs
# the libvoikko1 + voikko-fi system packages (installed in the Dockerfile).
# The app degrades to suffix-stripping if either is missing, so local dev on
# Windows/macOS without them still works.
libvoikko==4.3
